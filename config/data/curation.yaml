# @package data.curation
strategy: "naive"  # naive, curriculum, multitask
easy_sample_percentage: 50
num_sets_per_problem: 3
set_size: 8

verification:
  library: "math_verify"
  timeout: 30
  
output:
  train_split: 0.9
  validation_split: 0.1
  format: "parquet"

